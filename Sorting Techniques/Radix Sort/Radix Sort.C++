#include<bits/stdc++.h>
using namespace std;
//Function to sort element using radix sort
void radix sort()
{
  
}
int main()
{
  //Firstly Take the input
  vector<int> v;
  for(int i=0;i<n;i++)
  {
    int x;
    cin>>x;
    v.push_back(x);
  }
  
}
